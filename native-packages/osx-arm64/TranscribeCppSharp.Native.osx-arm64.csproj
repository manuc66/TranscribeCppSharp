<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <PackageId>TranscribeCppSharp.Native.osx-arm64</PackageId>
    <Description>Native transcribe.cpp binaries for macOS ARM64 (Metal) Built from upstream source and owned by the transcribe.cpp authors (MIT); this package only redistributes them.</Description>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <Authors>transcribe.cpp authors; ggml authors; miniz authors</Authors>
    <Copyright>Copyright (c) The transcribe.cpp authors, The ggml authors, and miniz authors</Copyright>
    <PackageProjectUrl>https://github.com/manuc66/TranscribeCppSharp</PackageProjectUrl>
    <RepositoryUrl>https://github.com/manuc66/TranscribeCppSharp</RepositoryUrl>
    <IsPackable>true</IsPackable>
    <!-- The global Directory.Build.props enables symbol packages (snupkg);
         a content-only package with no assemblies has nothing to symbolize,
         so NU5017 would fail the pack. -->
    <IncludeSymbols>false</IncludeSymbols>
  </PropertyGroup>

  <ItemGroup>
    <None Include="runtimes\**\*" Pack="true" PackagePath="runtimes" Exclude="runtimes\**\.done" />
  </ItemGroup>

</Project>
